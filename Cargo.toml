[package]
name = "reown"
version = "0.1.0"
edition = "2024"
description = "Own your codebase again, even in the age of agent PR storm."

[dependencies]
git2 = "0.20.4"
ratatui = "0.30.0"
crossterm = "0.29.0"
anyhow = "1.0"

[dev-dependencies]
tempfile = "3.25.0"
